[project]
name = "dcnet_permission"
authors = [
    { name = "DCNET", email = "admin@dcnet.vn" },
]
description = "Department-scoped user and role delegation for DCNET Flow"
requires-python = ">=3.10"
dynamic = ["version"]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "dcnet_permission"
