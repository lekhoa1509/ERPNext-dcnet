[project]
name = "vn_accounting"
authors = [
    { name = "DCNet", email = "info@dcnet.vn" }
]
description = "Vietnamese Accounting for ERPNext v16 — COA templates per TT99/2025, company defaults, workspace, sidebar, dashboard"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
